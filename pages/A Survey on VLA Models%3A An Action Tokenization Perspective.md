- VLA Future
	- Language Description
		- leverage high-level planning, decompose complex tasks to subtasks -> combining other forms of token ( affordance, trajectory, goal state)
	- Code
		- Full API development -> dynamic API development ?
		- robust action primitives -> local action robustness -> point cloud + segmentation
		- between high level + low level
	- Affordance
		- from 2D to 3D -> conflicts with World Model?
		- affordance evolves over time -> why not just calculate again?
		- robustness and uncertainty -> local action robustness + noise
	- Trajectory
		- 3D spatial understanding: point only -> matrix based trajectory ?
		- computational efficiency: need a full trajectory -> no, we dont need a full, we need dynamic planning all the time
		- task suitability: only for guidance, no interaction -> local motion issue?
	-